Tu opères dans un mode "Token-Saver" strict. Ton efficacité dépend de ta capacité à naviguer via la carte plutôt que par exploration brute.

### 🚫 ACTIONS INTERDITES (CONTRÔLE DES COÛTS)
- Ne lance JAMAIS de recherche globale (`grep`, `ls -R`, `find`) sans avoir consulté la carte.
- Ne lis JAMAIS plus de 2 fichiers sources simultanément avant d'avoir validé leur pertinence via le rapport d'architecture.
- **Ne scanne JAMAIS récursivement le projet** (`ls -R`, `find .`, `grep -r`) sans un objectif de recherche précis et validé.

### ✅ WORKFLOW OBLIGATOIRE (CHAIN OF THOUGHT)
1. **Étape de Navigation (Obligatoire) :** Pour toute demande nécessitant une compréhension de l'architecture, ta PREMIÈRE action doit être : `read_file("graphify-out/GRAPH_REPORT.md")`.
2. **Étape d'Analyse :** Utilise les relations listées dans le rapport pour identifier les "Points d'Entrée" et les "Dépendances" de la fonctionnalité concernée.
3. **Étape d'Extraction :** Ne demande la lecture des fichiers `.ts`, `.py`, `.go` etc., qu'une fois que tu as la preuve (via le rapport) qu'ils contiennent la logique ciblée.

### 🏠 VAULT OBSIDIAN — NAVIGATION STRATÉGIQUE
- Le projet est désormais un **Vault Obsidian complet**.
- Consulte `00_Dashboard.md` comme **point d'entrée stratégique** en complément du rapport technique.
- Le Dashboard contient l'accès rapide aux scripts principaux, les notes de tâches, et la navigation par communautés Graphify.
- Les filtres d'exclusion Obsidian (`node_modules/`, `.git/`, `dist/`, `build/`, `.next/`, `package-lock.json`, `uploads/`, `prisma/migrations/`) empêchent l'indexation des dossiers lourds.

### 🔄 MAINTENANCE DU GRAPHE (tu dois obligatoirement faire cela à la fin de chaque tâche)
- à la fin de chaque tâche tu DOIS obligatoirement veiller à exécuter la commande `graphify update` immédiatement après tes modifications pour garder ta "carte" à jour. Tu vérifiera systématiquement lors de ton récapitulatif de fin de tâche.

### 📊 VALIDATION DU GRAPHE
- Vérifie régulièrement que le nombre de "nodes" dans `GRAPH_REPORT.md` reste dans la centaine (50–150 nodes). Si le nombre dépasse 500, c'est que la plomberie d'une librairie (Prisma, etc.) pollue le graphe.
- Les fonctions métier (comme `sendGmail`, `sendEmail`, `generateEmail`, `server.js`, `requireAuth`, etc.) DOIVENT apparaître dans le rapport. Si ce n'est pas le cas, le `.graphifyignore` bloque peut-être trop de dossiers.

### ⚡ ASTUCES PERFORMANCE
- Pour chercher une définition précise, utilise `list_code_definition_names` sur le dossier parent plutôt qu'un `grep` global.

### 📝 PROTOCOLE DE FIN DE TÂCHE - HAUTE PRIORITÉ
Il est strictement obligatoire de générer une note de synthèse dans Obsidian (via Graphify) à la fin de chaque intervention. Cette note doit récapituler :
Les modifications effectuées
Les décisions prises
L'état actuel du système
Objectif : persistance du contexte hors-token.
